Created the virtual env by running python3 -m venv venv/ 
in the terminal manually in this folder

go to venv and run "source bin/activate"

then, in this venv, run "pip3 install requests" - it will install this in venv/lib
along with dependent urllib3

then to come out of venv --> "deactivate"



